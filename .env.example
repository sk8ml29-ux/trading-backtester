# Copy to .env and fill in values (never commit .env)

# Polygon.io — stocks/ETFs intraday (15m/30m/1h). Without this, stocks fall back to Yahoo.
POLYGON_API_KEY=

# Optional alias (either name works)
# POLYGON_KEY=
