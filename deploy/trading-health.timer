[Unit]
Description=Kör trading-health.service en gång per dag (07:00 UTC)

[Timer]
# Varje dag 07:00 UTC (~09:00 svensk tid). OnCalendar=daglig.
OnCalendar=*-*-* 07:00:00
Persistent=true

[Install]
WantedBy=timers.target
